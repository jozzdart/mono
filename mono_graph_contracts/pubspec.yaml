name: mono_graph_contracts
description: "Dependency graph contracts and algorithms for Mono (contracts-only)"
resolution: workspace
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta:
  collection:
  mono_core_types:

dev_dependencies:
  test:
  lints:
