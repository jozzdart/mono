name: terminice
description: "Beautiful, interactive CLI widgets and prompts for terminal apps"
version: 0.0.1
homepage: https://github.com/jozzdart/mono
repository: https://github.com/jozzdart/mono/tree/main/terminice
issue_tracker: https://github.com/jozzdart/mono/issues
topics:
  - cli
  - terminal
  - prompt
  - menu
  - interactive

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta: ^1.15.0
  collection: ^1.18.0
  intl: ^0.20.2

dev_dependencies:
  test:
  lints:
