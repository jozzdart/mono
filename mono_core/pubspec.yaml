name: mono_core
description: "Mono Core"
resolution: workspace
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta:
  collection:

dev_dependencies:
  test:
  lints:
