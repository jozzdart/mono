name: mono_core
description: "Mono Core"
resolution: workspace
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta: ^1.15.0
  collection: ^1.18.0

dev_dependencies:
  test:
  lints:
