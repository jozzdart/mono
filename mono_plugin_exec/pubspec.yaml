name: mono_plugin_exec
description: Exec plugin to run arbitrary shell commands.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta:
  mono_core_types:
  mono_ports:

dev_dependencies:
  lints:
  test:
