name: mono_core_types
description: "Core value types and domain primitives for Mono (contracts-only)"
resolution: workspace
version: 0.0.1
homepage:

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta:
  collection:

dev_dependencies:
  test:
  lints:
