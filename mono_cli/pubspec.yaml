name: mono_cli
description: CLI exports and implementations for the Mono CLI
resolution: workspace
version: 0.0.1
homepage: https://github.com/jozzdart/mono
repository: https://github.com/jozzdart/mono/tree/main/mono_cli
issue_tracker: https://github.com/jozzdart/mono/issues
topics:
  - mono
  - monorepo
  - cli
  - tool

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  mono_core:
  args: ^2.5.0
  yaml: ^3.1.2
  glob: ^2.1.2
  path: ^1.9.0
  pool: ^1.5.2

dev_dependencies:
  lints:
  test:

executables:
  mono: mono
