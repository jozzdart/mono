name: mono
description: The fastest and most flexible CLI for managing Dart/Flutter monorepos, tasks, scanning, groups, and more.
resolution: workspace
version: 0.0.2
homepage: https://github.com/jozzdart/mono
repository: https://github.com/jozzdart/mono/tree/main/mono
issue_tracker: https://github.com/jozzdart/mono/issues
topics:
  - mono
  - monorepo
  - cli
  - tool

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  mono_cli: ^0.0.2

dev_dependencies:
  lints:
  test:

executables:
  mono: mono
