name: mono
description: Public-facing package that re-exports mono CLI, contracts, implementations, and plugins.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta:
  yaml:
  mono_core:
  mono_core_types:
  mono_graph_contracts:
  mono_selector_contracts:
  mono_ports:
  mono_config_contracts:
  mono_cli_shared_contracts:
  mono_cli:
  mono_cli_parser_args:
  mono_config_yaml:
  mono_scanner_fs:
  mono_graph_builder_impl:
  mono_selector_impl:
  mono_runner:
  mono_system_io:
  mono_plugin_pub:
  mono_plugin_exec:

dev_dependencies:
  lints:
  test:
