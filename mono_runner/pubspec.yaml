name: mono_runner
description: Command planner and execution engine for mono CLI.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  async:
  pool:
  meta:
  mono_ports:
  mono_core_types:

dev_dependencies:
  lints:
  test:
