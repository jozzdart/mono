name: mono_cli_shared_contracts
description: "Shared CLI AST, usage, completion contracts for Mono"
resolution: workspace
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  meta:
  mono_selector_contracts:
  mono_core_types:

dev_dependencies:
  test:
  lints:
